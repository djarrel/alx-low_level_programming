#include "main.h"

/**
 * print_last_digit - prints the last digit
 * @x: holds the values
 * Return: r is value of the last digit
*/

int print_last_digit(int x)
{
	int d;

	d = x % 10;
	return (d);
}
